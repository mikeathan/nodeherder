services:
  backend:
    container_name: nodeherder-backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "4110:4110"
    env_file:
      - ./backend/.env
      - ./backend/.env.production
    volumes:
      - ./backend/configs:/backend/configs
      - ./backend/data:/backend/data
    environment:
      APP_ENV: production
    depends_on:
      - mqtt
      - zigbee2mqtt
    networks:
      - nodeherder

  mqtt:
    image: eclipse-mosquitto:2
    container_name: nodeherder-mqtt
    restart: unless-stopped
    # ports:
    #   - "1883:1883"
    volumes:
      - ${DATA_ROOT}/mqtt/data:/mosquitto/data
      - ${DATA_ROOT}/mqtt/config:/mosquitto/config
      - ${DATA_ROOT}/mqtt/log:/mosquitto/log
    networks:
      - nodeherder

  zigbee2mqtt:
    image: koenkk/zigbee2mqtt
    container_name: zigbee2mqtt
    restart: unless-stopped
    volumes:
      - ${DATA_ROOT}/zigbee2mqtt-data:/app/data
      - /run/udev:/run/udev:ro
    ports:
      - "8089:8080"
    environment:
      - TZ=Europe/London
    devices:
      - ${Z2M_DEVICE}:${Z2M_SERIAL_PORT}
    depends_on:
      - mqtt
    networks:
      - nodeherder

networks:
  nodeherder:
    driver: bridge
