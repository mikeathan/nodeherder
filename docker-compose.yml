services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: nodeherder-frontend
    ports:
      - "8081:80"
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
