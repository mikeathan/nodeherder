sudo nano /etc/systemd/system/nodeherder.service

journalctl -b | grep nodeherder
sudo systemctl daemon-reload

git config --system --unset credential.helper
git config --global credential.helper cache

// zigbee2mqtt/bridge/devices

// https://www.home-assistant.io/docs/automation/basics/
// https://www.home-assistant.io/docs/automation/editor/
// mosquitto_pub -h localhost:1883 -u mqttuser -P mqttpass -t 'zigbee2mqtt/Hive light 1/set' -m '{ "state": "ON" }'

// mosquitto_pub -h localhost:1883 -u mqttuser -P mqttpass -t 'zigbee2mqtt/bridge/devices'

mosquitto_sub -h localhost -u mqttuser -P mqttpass -v -t "#"

mosquitto_sub -h localhost -u mqttuser -P mqttpass -v -t "#"
user@localhost:~$ mosquitto_sub -h localhost -u mqttuser -P mqttpass -v -t "zigbee2mqtt/Human presence"
